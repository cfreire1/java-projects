myapp:
  validation:
    validation-groups:
      - com.mycompany.mypackage.MyGroup
      - javax.validation.groups.Default
    ignore-unknown-properties: false
    message:
      validation-failed: "La validaci�n fall�. Verifique los errores de validaci�n."
      invalid-data: ${DATA_TEXT:https://domainlocal.error}
  user:
    min-age: 18
    max-age: 99
    default-email: john.doe@example.com
